<!DOCTYPE html>
<html ng-app="LumneeApp">
<head>
  <title>Lumnee App</title>
  
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="login-logoff-and-dropdown">

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <!-- Google links may need to be added here -->
   <% if user_signed_in? %>
      <%= current_user.email %>
      <br>
      <%= link_to "Edit profile", edit_user_registration_path %>
      <br>
      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path %>
      <br>
      <%= link_to "Login", user_session_path %>
      <br>
      <%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2) %>
    <% end %>
    <!-- Might not need the notice below, it was originally on the user and also on the event show page. -->
    <p id="notice"><%= notice %></p>

    <!-- Drop down menu 1 (two does not work with the code for now so I may need to wrap it in a separate function. -->
    <div class="btn-group" dropdown is-open="status.isopen">
      <button type="button" class="btn btn-primary dropdown-toggle" ng-disabled="disabled">
        Button dropdown <span class="caret"></span>
      </button>
      <ul class="dropdown-menu" role="menu">
        <li><a href="#">Action</a></li>
        <li><a href="#">Another action</a></li>
        <li><a href="#">Something else here</a></li>
        <li class="divider"></li>
        <li><a href="#">Separated link</a></li>
      </ul>
    </div>

  </div>
  <!-- There as a flash message in the notes but it's probably not crucial -->
  <div ng-controller="LumneeController">

    <%= yield %>
    
  </div>
</body>
</html>
